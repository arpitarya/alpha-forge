/**
 * Solar Orb Components — re-exported from @alphaforge/solar-orb-ui package.
 *
 * Usage:
 *   import { Button, Input, Card, Badge, Icon, Text } from "@/components/solar-orb";
 *   import { tokens, color } from "@/components/solar-orb";
 *   // or directly:
 *   import { Button } from "@alphaforge/solar-orb-ui";
 *   import { tokens } from "@alphaforge/solar-orb-ui";
 */

export {
  Badge,
  Button,
  Card,
  Icon,
  Input,
  Text,
  tokens,
  color,
  font,
  spacing,
  radius,
  shadow,
  blur,
  animation,
  layout,
  zIndex,
} from "@alphaforge/solar-orb-ui";

export type {
  BadgeProps,
  ButtonProps,
  CardProps,
  IconProps,
  InputProps,
  TextProps,
  Tokens,
} from "@alphaforge/solar-orb-ui";
