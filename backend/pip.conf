# AlphaForge — pip configuration (Python equivalent of .npmrc)
# Applied when pip runs inside the project virtualenv

[global]
# Require a virtualenv to be active — prevent accidental global installs
require-virtualenv = true

# Timeout for network requests (seconds)
timeout = 60

[install]
# Compile bytecode during install for faster startup
compile = true
